// add of two number
#include <stdio.h>

int main()
{
    int a, b, total;
    printf("Enter the two number:");
    scanf("%d %d", &a, &b);

    total = a + b;
    printf("here is the total: %d", total);
    return 0;
}