/*
Here we have simple add the two number.
*/
#include <stdio.h>

int main()
{
    int i, j, add;
    printf("Enter the two number: - ");
    scanf("%d %d", &i, &j);

    add = i + j;
    printf("Addition of the two number is  %d", add);
    return 0;
}